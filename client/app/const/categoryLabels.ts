import {AircraftCategory} from "@/app/lib/types";

const categoryLabels: Record<AircraftCategory, string> = {
    plane: "Samoloty",
    glider: "Szybowce",
    baloon: "Balony"
};

export default categoryLabels;